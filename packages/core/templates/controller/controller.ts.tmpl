import { Controller, Request, Response, Route } from '@orbital/core';

@Controller()
export class %CAMEL_CASE%Controller {

    @Route({
        method: 'post'
    })
    sayHello(req: Request, res: Response) {
        res.status(200).send(`Hello, ${req.body.name}`);
    }

}
